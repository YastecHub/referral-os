# ReferralOS Frontend

Fresh React/Vite frontend with reusable components, shared healthcare shell, admin Command Centre, responsive layouts, mock data and a backend-ready API/service boundary.

## Run
npm install
npm run dev

## Routes
/ /signin /signup /dashboard /sending /receiving /admin

Use an email containing `admin` on sign-in for the admin demo, or use “Sign in as Admin”.
